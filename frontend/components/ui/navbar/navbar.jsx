import { useState } from 'react';
import { SearchIcon, BellIcon, MenuIcon } from '@heroicons/react/outline'; // Import icons as needed

const Navbar = ({ onSidebarOpen }) => {
  const [searchInput, setSearchInput] = useState('');

  return (
    <div className="bg-primary h-16 flex justify-between items-center px-4 shadow-md">
      
    </div>
  );
};

export default Navbar;
